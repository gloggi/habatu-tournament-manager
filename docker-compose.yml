version: "3"
services:
       habatu-backend:
           build: ./habatu-backend
           volumes:
            - ./habatu-backend:/usr/app
            - /usr/app/node_modules
           command: npm run dev
           ports:
             - "8000:8000"
           links:
             - mongo
       mongo:
             container_name: mongo
             image: mongo
             volumes:
               - ./data:/data/db
             ports:
               - '27018:27017'